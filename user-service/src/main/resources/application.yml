spring:
  application:
    name: userservice
springdoc:
  api-docs:
    enabled: true
eureka:
  client:
    # 需要指向eureka注册中心的地址
    service-url:
      defaultZone: http://10.22.3.10:8801/eureka/, http://10.22.3.10:8802/eureka/
  instance:
    appname: userservice